{
  "name": "my-core",
  "version": "1.0.0",
  "description": "WordPress core development with Wokenv",
  "private": true,
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:cli": "wp-env run cli",
    "env:test": "wp-env run tests-cli phpunit"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
