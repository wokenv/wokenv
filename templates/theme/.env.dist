# Wokenv Environment Variables
# Copy this file to .env and customize as needed
# .env is gitignored and contains local-specific values

# Project name (used for container naming and network)
COMPOSE_PROJECT_NAME=my-theme

# User/Group IDs for file permissions
# Auto-detected by wokenv, override only if needed
USER_ID=1000
GROUP_ID=1000

# Optional: Override Docker image
# See: https://hub.docker.com/r/frugan/wokenv/tags
# WOKENV_IMAGE=frugan/wokenv:node20-alpine-wpenv10

# Optional: Service ports
# Mailpit email catcher
MAILPIT_WEB_PORT=8025
MAILPIT_SMTP_PORT=1025

# phpMyAdmin database manager
PHPMYADMIN_PORT=9000
